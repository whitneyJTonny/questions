#using a function, create a program that calculates the volume of a cyclinder

import math

radius=int(input("Enter the radius of the cylinder"))
height=int(input("Enter the height of the cyclinder"))
pie= math.pi
pie=22/7
volume=pie * radius**2 *height
print(volume)